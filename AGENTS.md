# Agent 工作约定

## 基本要求

- 默认工作目录是 `/Users/liuqingxing/Desktop/小说生成器`，操作文件前确认路径。
- 中文沟通，说明功能层面的变化和验证结果，少讲代码细节。
- 调外部 API、CLI 或模型能力前，先确认当前项目代码或官方文档里的真实端点、模型名和参数。
- 发现测试失败、验证缺失、实现风险或用户前提有问题时，直接说明，不美化结果。
- 不还原用户已有改动，不使用 `git reset --hard`、`git checkout --` 等破坏性命令，除非用户明确要求。
- 查找文件和文本优先使用 `rg` / `rg --files`；手工编辑文件使用 `apply_patch`。

## 验证要求

- 报告完成前必须执行与改动范围匹配的验证。
- 代码改动优先执行相关单测；影响较大时执行 `npm run verify`。
- 前端体验改动需要执行相应 UI smoke 或浏览器检查。
- 验证失败或无法验证时，在最终回复和 `CHANGELOG.md` 中写明实际状态。

## 项目知识同步

- 每次完成有实际价值的项目修改后，最终回复前使用 `neat-freak` skill 检查项目说明。
- 检查范围包括本文件、`README.md`、`docs/`、`CHANGELOG.md`。
- 当前事实写入 `README.md`、`docs/` 和本文件；历史记录写入 `CHANGELOG.md`。
- `CHANGELOG.md` 每条有效记录包含绝对日期、修改摘要、影响范围和验证结果。

## 当前项目事实

- 本项目是本地长篇小说生成器，前端使用 Vue 3 + Vite，后端使用 FastAPI，桌面壳使用 Tauri 2。
- 作品文件、章节、设定、Obsidian 配置和执行历史保存在作品目录；资料索引使用 `SQLite / FTS5`。
- 模型设置面向普通作者只暴露写作模型预设、自定义接口、API Key 和篇幅能力；第二审查模型、自动修订和运行调度在高级区展开；设置页不再显示 Embedding 配置入口。Embedding 默认使用随 sidecar 打包的本地 `local-fastembed` / `BAAI/bge-small-zh-v1.5`，模型文件位于 `backend/novel_backend/assets/embedding_models/fast-bge-small-zh-v1.5`，维度 512，不需要用户填写 Embedding API Key；旧配置或直接后端 API 传入云端 OpenAI-compatible `/embeddings` 时仍可兼容读取。“测试当前配置”会用当前表单值测试写作模型、内置知识检索模型和已启用的第二审查模型，不需要先保存。`ModelConfig.temperature` 和 `ReviewModelConfig.temperature` 只作为旧配置兼容字段保留，新设置页不再写入；聊天模型请求在传输层会移除 `temperature / top_p` 这类可选采样项，避免部分 OpenAI-compatible 模型拒收；SSL EOF、远端中途断开、临时连接错误和可重试 5xx / 429 会自动重试，聊天模型请求和设置页模型测试默认最多重试 5 次，可用 `NOVEL_MODEL_RETRY_DELAYS` 配置等待间隔；自动修订默认最多 2 轮。
- 旧稿接管用于把已经写了一部分的小说变成系统章节正文；前端入口是作品列表顶部“旧稿”，文件上传只支持 `.txt`，也可以直接粘贴正文；后端接口是 `POST /api/projects/takeover/import`、`GET /api/projects/{project_id}/takeover` 和 `POST /api/projects/{project_id}/takeover/resume`。接管状态、原稿副本、拆章结果和接管报告保存在 `.gaoxia/takeover/`；导入会逐章写入 `chapters/`，把接续位置、上一章结尾、最近章节和写作边界写入 `core_seed.txt`、`plot_structure.txt`、`character_state.txt`、`blueprint.txt`、`global_summary.txt` 与 `checkpoint.json`，再刷新知识库。恢复接管时会保留已有非空章节正文，不用旧稿覆盖作者已改内容；前端会在读取前拒绝 30MB 以上旧稿文件；当前项目章节上限是 1000 章。旧稿接管不等同于资料导入，导入阶段不逐章触发模型核验。
- Agent 执行消息会保存 `execution_trace / event_blocks / artifacts`；前端会把 `event_blocks` 按计划阶段、执行阶段和结果阶段展示，方便作者回看一次长任务的计划、动作和最终结果。Agent workflow 状态支持按 `task_id` 查询；前端停止长任务时会向后端写入中断请求，执行循环在动作边界把后续动作标记为 `CANCELLED`。
- Agent 主对话请求支持长文本输入；`AgentMessage.content` 和线程消息都可保存最多 1000000 字符，前端保留最新用户长输入并压缩旧历史；超过 20000 字符的用户输入会由后端先筛掉明显无关、技术日志或网页样板段落，再按约 50000 字符分段导入项目资料库，本轮计划和执行会携带这些资料引用；路由、规划、brainstorm、技能整理和执行轨迹使用“摘要 + 原文头尾 + 资料引用”的压缩文本，避免把完整长文本直接送进模型上下文。
- 项目迁移使用 `.gaoxia-project.zip` 迁移包，导出时打包完整作品目录并写入 `exports/`，导入时复制到当前工作区、注册到作品列表；前端导入入口在工作台右上角“更多”菜单，导出入口在工作台“更多”菜单和作品菜单；同机重复导入不会覆盖原作品，会生成新的作品 ID。项目目录外的 Obsidian Vault 原文不会被迁移包复制；导出包会保留项目内 Obsidian 配置和章节学习状态，外部 Vault 的同步文件只保留配置、空统计和重新同步提示，不携带笔记列表、摘要或预览；包内 `narrative_state.json` 会移除 Obsidian 维护建议和动作，`.gaoxia/obsidian_drafts/` 维护草稿不会进入迁移包；包内 `project_distillation.json` 会移除外部 Obsidian 蒸馏条目并标记为需要重建；`.gaoxia/learning/*.json/.jsonl` 和其它 `.gaoxia` 状态里的外部 Obsidian 资料分析、自学习复盘、失败案例文本会改成迁移提示；Agent 线程和 `.gaoxia/runs/` workflow 状态会保留，但其中的 Obsidian 资料分析 artifact、维护 artifact、相关 trace / event、action / subtask 摘要会改成迁移提示，可重建的 `.gaoxia/thread_context/` 索引不会进入迁移包；打包用的 `knowledge.db` 副本也会移除外部 Obsidian 索引内容，导入后按当前环境重新建索引。
- PDF 资料导入在 `qwen-doc-turbo` 不可用或失败后，会优先尝试 LiteParse 本地解析并按页加入 `【第 N 页】` 标记；LiteParse 未安装、解析失败或无正文时回到 `pypdf`，只有本地文本为空时才尝试 LiteParse OCR，OCR 语言可用 `NOVEL_LITEPARSE_OCR_LANGUAGE` 配置。
- Obsidian `include_patterns / exclude_patterns` 按大小写不敏感匹配；默认排除 `.obsidian/**`、`.trash/**` 和 `templates/**` 时，也会排除 `.OBSIDIAN/`、`.Trash/`、`Templates/` 等常见目录写法；作者自定义 `drafts/**` 这类规则时也会匹配 `Drafts/`；候选笔记超过 `max_notes` 时，会先按 Vault 相对路径稳定排序再应用数量上限，并在同步警告里提示有候选笔记未同步；候选总数和 `max_notes` 会进入来源签名，让界面的 skipped 和警告随 Vault 增删更新。
- Obsidian Vault 是可选资料源，默认读取 Markdown、Canvas、frontmatter、正文内联属性、摘要、检索词、双链、Markdown 内链、Canvas file 节点和边、标签、别名、章节范围、剧透边界、按常见章节标签、文件名或路径推断的章节范围、必需短语和禁用短语；`summary / description / abstract / keywords / search_terms / 关键词` 等 Properties 以及 `summary:: / keywords::`、`[summary:: ...]`、`(keywords:: ...)` 内联属性会进入知识索引、笔记选择和章节安全预览；frontmatter 字段名支持大小写、空格、连字符和下划线等常见属性写法，frontmatter 或正文内联属性里的 `source_notes / related_characters / related_notes / depends_on / foreshadows / payoffs / reveals / related_locations / related_props / related_organizations` 等关系字段会参与图谱解析，并把依赖、伏笔、兑现、揭示、相关地点等关系语义保留到知识索引、章节上下文和界面预览；Canvas file 节点和边会生成可解析链接、反向链接和关系语义；Canvas file 节点关系进入章节上下文时，会优先显示目标笔记标题和可见来源笔记标题，而不是原始文件路径；章节上下文只展示目标章节可见的关系目标，当前笔记正文或摘要里指向未来笔记的 `[[双链]]`、Markdown 内链或 Canvas file 节点会改写为“未开放设定”，真正未解析或歧义的双链仍作为图谱风险提示，避免早期章节通过关系标题、正文链接、Markdown 链接路径或 Canvas 关系看到后段笔记；明确绑定目标章节的笔记会优先进入章节上下文和 Agent 资料分析，章节上下文、Agent 资料分析、任务蒸馏、检索预览、连续性证据正文、叙事状态账本和核验签名按目标章节处理未来笔记；有目标章节时，知识检索和证据检索会先读取更大的候选池再按章节过滤，章节上下文触发的知识检索也会传入目标章节，Obsidian 检索命中如果无法对应到当前总览里的可见笔记，会被丢弃；技能库、架构总览和联网考据会把当前选中章节传给后端，有选中章节时只返回或引用该章节安全预览；章节核验里的 Obsidian 必写 / 禁写问题会进入自动修订触发条件。
- Obsidian frontmatter 的 YAML flow sequence 会保留引号内逗号，例如 `aliases: ["潮师, 守账人"]` 和 `required_phrases: ["潮声异常, 不得提前解释"]` 不会被误拆；普通列表型 Properties 和正文内联属性支持按逗号、顿号、半角分号和中文分号分隔多值，`tags: "人物；第58章"` 会拆成单个标签并继续参与章节范围识别。
- Obsidian frontmatter 支持 `summary: >`、`description: |`、`summary: >2-`、`keywords: |2-` 这类 YAML block scalar；多行摘要会进入知识索引、笔记选择和章节安全预览，多行 `keywords / source_notes / required_phrases` 等列表型字段会按行参与检索词、图谱关系和写作约束解析；这些列表字段的单项也可以写成 `- >` 或 `- |`，用于维护较长的检索词、来源笔记、必写项或禁写项。
- Obsidian frontmatter 支持 YAML 对象列表和顶层嵌套对象；章节计划、章节合同、剧情债务和人物弧线笔记把 `scenes / required_beats / character_checks / debts_to_advance` 写成 `- goal: ...`、`conflict: ...`、`payoff: ...` 这类多字段对象，写成单独 `-` 后换行维护对象字段，或用 `required_beats: [{goal: ..., evidence_sources: [{source_note: ...}]}]`、`- {goal: ..., payoff: ...}` 这类 YAML flow mapping 时，会转成章节上下文可读的结构化行；对象字段值可以用 `goal: >`、`reason: |` 这类 YAML block scalar 维护多行场景目标、理由或验收说明；对象里继续嵌套 `character_checks / evidence_sources` 等对象列表时也会递归解析，嵌套列表同样支持单独 `-` 后换行写对象字段；`chapter_contract:` 这类顶层对象里的 `chapter_range / objective / required_beats / acceptance_checks / evidence_sources` 等直接子字段也会进入章节范围、章节上下文和图谱关系；对象里的双链和 Markdown 内链会进入链接、反向链接和图谱关系统计，并继续按目标章节隐藏未来笔记。
- Obsidian 笔记没有显式 `type / kind / 类型` 时，会按常见目录或标签推断类型：`Characters/`、`Locations/`、`Plans/`、`Debts/`、`CharacterArcs/`、`Style/`、`XP/` 等会分别推断为人物、地点、章节计划、剧情债务、人物弧线、文风规则和 XP 规则；`#人物/主角`、正文 `#人物／主角`、`#章节计划/58`、`#剧情债务/伏笔` 这类层级标签会按半角或全角斜杠分段参与类型推断；显式 type 优先，Obsidian 多选 Properties 里的 `type: [主角, 人物]`、`type: [临时, 章节计划]` 会扫描整个列表并识别出系统已知类型，完全未知的显式 type 会保留作者写法，不会被目录或标签覆盖。
- Obsidian 章节计划类层级标签会同时提供章节范围：`#章节计划/58`、`#章节合同/58-60`、`#场景卡/59`、`#scene-plan/59` 会绑定对应章节，不需要额外写 `chapter_range`；普通 `#人物/主角`、`#剧情债务/伏笔` 不会被当成章节范围。
- Obsidian AI 可见性字段和标签支持正向、反向写法：`usable_by_ai / ai_usable / AI可用 / 可供AI使用 / 可供模型使用 / 写作可用` 或 `#AI可用` 为真时视为明确可用；`no_ai / not_for_ai / exclude_from_ai / AI不可用 / 不供AI使用 / 不允许AI使用 / 勿用AI`、`#no-ai` 或 `#AI不可用` 为真时会排除笔记，`no_ai: false` 或 `AI不可用: 否` 视为明确可用；Markdown frontmatter、正文内联属性、正文标签、HTML details 正文内联属性 / 标签和 Canvas 文本节点属性使用同一规则。HTML details 正文声明不可用于 AI 或声明过滤状态时只排除该折叠块；Canvas 按节点隔离 AI 不可用内容，单个 `no_ai:: true` 或 `#AI不可用` 文本节点不会让整张画布失效，`no_ai:: false` 不会误删公开节点。
- Obsidian 显式 `status / 状态` 字段、布尔状态 Properties、状态标签和目录会统一参与状态过滤：`status: 正式设定 / official / final`、`canonical: true / published: yes`、正文 `canonical:: true`、Canvas 文本节点 `canonical:: true`、`#canonical / #正式 / #已发布` 或 `正式设定/`、`Published/` 等会按正式可用状态处理，`status: wip / archived`、`draft: true / private: true / archived: true`、正文或 Canvas 文本节点 `draft:: true`、`#draft / #草稿 / #private / #私密 / #废案` 或 `Drafts/`、`草稿/`、`Private/` 等会按过滤状态处理；显式 `status` 字段优先，不被布尔属性、标签或目录覆盖。
- 目标章节上下文会对 Obsidian 元数据里的 `tags / aliases / keywords / required_phrases / forbidden_phrases` 进行章节安全处理；这些字段中的未来双链、Markdown 内链或已知未来笔记纯文本标题 / 路径名 / 文件名会改写为“未开放设定”，避免本章写作约束和标签预览提前暴露后段笔记标题。
- Obsidian frontmatter 的行尾 YAML 注释会被忽略，例如 `status: canonical # 正式设定`、`chapter_range: 58-60 # 中段` 不会污染状态、章节范围、图谱关系或写作约束；引号里的 `#` 和双链 heading 会保留。
- Obsidian `tags` / `tag` / `标签` 属性和正文标签支持常见 Obsidian 写法，例如 `tags: "#人物 #第58章 #剧透/57"`、`tags: #章节/44-45 #剧透/43`、多行 `tags: - #人物/配角`、`tags:: #支线 #第59章`、正文 `#人物／主角 #适用章节／40～42 #剧透／39` 会拆成单个标签，并用于标签展示、类型推断、章节范围和剧透边界识别；普通 frontmatter 字段仍按 YAML 行尾注释规则处理。
- Obsidian 章节范围支持开放式写法，例如 `chapter_range: 58+`、`chapter_range:: 第59章以后`、`tags: [第58章起]`、`tags: [Ch58+]`、正文 `#Ch58+` 会记录起始章节且不写截止章节，后续章节仍可引用；单章标签 `#第58章` 仍只绑定第 58 章。
- Obsidian 章节范围里的常见区间分隔符支持半角连字符、波浪线、中文全角波浪线、长横线和“至 / 到”，正文标签里的全角斜杠也会参与章节标签解析，例如 `#第58～60章`、`#Ch58～60`、`#适用章节／40～42`、`#剧透／39`、`第58～60章-计划.md` 会按对应章节边界过滤。
- Obsidian 笔记选择会把 `reveal_after_chapter`、`#剧透/57` 和 `#第57章后可用` 这类剧透边界作为章节相关性信号；目标章节开放后，章节档案和后段设定即使没有显式 `chapter_range` 也会优先进入上下文选择。
- Obsidian 可作为文风 / XP 资料源：`type: style_rule / xp_rule`、`Style/`、`XP/`、`文风/`、`写作规则/` 或相关标签命中的正式笔记，会在目标章节可见时进入章节生成、改稿、诊断、Agent 自动续写和 Agent 路由 / 规划上下文的项目级文风 / XP 提示；作者只在 Properties 里维护 `style_rule / voice_rule / tone_rule / sentence_rhythm / imagery / dialogue_rule / avoid_style / examples / applies_to` 或 `xp_rule / precheck / postcheck / workflow / technique / avoid_xp` 时，提示构建会读取同步后的结构化正文，把这些字段作为结构化规则进入提示，不依赖摘要或短预览截断；可见规则超过提示容量时，会优先保留目标章节范围最贴近的规则，并在容量允许时保留文风和 XP 各自最相关条目，避免通用规则挤掉本章专属规则；没有目标章节时也会从同步记录读取无章节边界的全局规则，并排除带章节范围或剧透边界的后段规则，且不会写入 `.gaoxia/learning/style_xp_evolution.json`。系统学习版文风 / XP 规则在至少两个章节重复出现且没有匹配到 Vault 规则笔记时，会生成 `Style/` 或 `XP/` 低优先级 Obsidian 待审维护草稿，草稿带来源规则、来源章节、按最晚来源章生成的剧透边界、`source_ids` 和 `style_rule` 或 `xp_rule` Properties；未发布前会在章节生成上下文、章节化 `brainstorm` 讨论上下文和 Agent 规划上下文显示非正式文风 / XP 预览，摘取规则、适用范围、检查项、证据数和置信度；发布后才作为 Vault 正式规则参与后续提示。发布后的规则笔记如果保留 `source_ids`，作者改名、移动或重写规则表达后，也不会重复生成同一条维护建议。
- Obsidian 可作为章节档案来源：`type: chapter_note / chapter_summary / chapter_archive / author_archive`、`ChapterNotes/`、`章节档案/` 或相关标签命中的正式笔记，会按来源章节和剧透边界进入后续章节检索；作者只在 Properties 里维护 `chapter_title / chapter_summary / chapter_events / state_changes / handoff_to_next / chapter_excerpt` 时，这些字段也会进入知识索引、章节上下文、Agent 能力上下文和叙事状态账本的“Obsidian 章节档案”提示；章节核验会检查明确指向当前章的强制类 `handoff_to_next`，正文没有承接可核验关键词时会作为 Obsidian 必需设定问题进入自动修订判定。系统生成的章节档案草稿会写入 `chapter_index / chapter_title / chapter_summary / handoff_to_next / chapter_excerpt / obsidian_required_satisfied / obsidian_required_missing / obsidian_forbidden_violations`，发布后可直接作为后续章节的结构化回顾来源；其中“下一章关注本章后果”类交接只进入上下文，不触发自动修订。
- Obsidian 可作为章节计划来源：`type: chapter_plan / scene_plan / chapter_contract`、`Plans/`、`Scenes/`、`章节计划/`、`场景卡/` 或相关标签命中的正式笔记，会在目标章节明确且章节范围很窄时进入叙事状态账本和模型叙事编辑的下一章合同输入；后段章节计划不会进入早期章节提示。模型叙事编辑生成的下一章章节合同如果没有匹配到可用 Vault 计划或合同笔记，会生成 `Plans/` 中优先级 Obsidian 待审草稿；草稿带 `type: chapter_contract`、目标章节范围、来源章节、`source_ids` 和 `gaoxia_maintenance_id`，未发布前会在目标章节待审草稿提醒里显示非正式合同预览，摘取章节目标、必须节拍、禁止动作和验收项，发布后才作为 Vault 正式章节计划参与目标章节提示；发布后的合同笔记保留 `gaoxia_maintenance_id` 或 `source_ids` 时，作者改写正文仍按同一合同识别；已发布的 `chapter_contract` 会按合同小节读取章节目标、节拍、债务、人物检查、文风检查、禁止动作和验收项，避免提示只拿到正文前几行；作者只在 Properties 里维护 `objective / required_beats / debts_to_advance / debts_to_protect / character_checks / style_checks / forbidden_moves / acceptance_checks / evidence_sources / risk_notes` 时，这些字段也会转成章节计划行，`evidence_sources` 会形成图谱关系。
- Obsidian 可作为剧情债务 / 人物弧线来源：`type: narrative_debt / plot_debt / character_arc / character_state`、`Debts/`、`PlotDebts/`、`CharacterArcs/`、`剧情债务/`、`人物弧线/` 或相关标签命中的正式笔记，会按目标章节可见范围进入叙事状态账本；Agent 路由 / 规划上下文会读取目标章节可见的 Obsidian 来源、章节计划、必写项、禁写项、剧情债务、人物弧线和图谱风险；生成章节前可作为上下文约束，章节保存后会写入 `.gaoxia/learning/narrative_state.json` 的债务或人物弧线来源，也会写入章节任务卡并显示在自学习面板的最新章节任务卡里，模型叙事编辑生成下一章合同时也会读取下一章可见的这些条目。作者只在 Properties 里维护 `debt_content / debt_status / risk_level / expected_payoff_range / next_required_action / related_characters`，或 `character / phase / current_state / unresolved_pressure / required_next_check` 时，这些字段也会进入知识索引、账本和任务卡；系统自动生成的剧情债务草稿会写入债务 Properties，人物状态草稿会写入人物状态 Properties 并带 `人物状态` 标签，发布后的笔记带 `source_ids`，作者改名、移动或改写标题后，只要保留来源 ID，就不会重复生成同一条维护建议。
- Agent 路由 / 规划上下文支持多章节目标；“第 58 到 60 章”“第 58 章到第 60 章”会被解析成多个目标章节，“先检查第一章，再生成第二章”会优先使用生成、改稿、拆场或诊断目标；包含“蓝图 / 架构”等词但明确写了章节范围时，也会优先按目标章节读取 Obsidian 任务。能力上下文最多展开前 3 个目标章节的 Obsidian 任务摘要，包含来源、章节计划、章节档案、必写项、禁写项、剧情债务、人物弧线和图谱风险，维护摘要、维护建议会按这些目标章节共同筛选、统计和排序；明确要求生成正文的章节范围时，即使规划模型只返回一个正文动作，后端也会把 2 到 3 章的小批量范围展开成逐章生成动作，再分别进入去 AI 和一致性复查；超过 3 章的直接正文生成请求会提示分批或先整理章节蓝图；执行阶段如果 `review_knowledge` 先按第 58 章分析资料，后续动作切到第 59 / 60 章时会重新生成该章资料摘要并缓存给同章生成、改稿和复查使用。
- 内置技能行为由后端统一注册，并在 `SkillBehavior` 里记录 `agent_action_kind / agent_action_mode / agent_requires_confirmation`；技能目录初始化和旧 JSON 读取会合并缺失的 Agent 元数据，前端 fallback 也保持同样字段。Agent 规划器看到的技能目录会标出对应 action 和确认要求，例如 `chapter_workflow/draft`、`rewrite_chapter/humanize` 和 `consistency_check`。主对话快捷按钮会把匹配的内置技能 ID 放进 `active_skill_ids`，后端会用这些元数据修正计划动作，例如把“续写本章”路由到 `chapter_workflow/draft`，把“去 AI”路由到 `rewrite_chapter/humanize`；内置技能 ID 不会当成用户自定义技能 prompt 注入执行。
- Agent 资料分析 `review_knowledge` 继承目标章节时，会读取 `build_project_context_bundle()` 的章节安全上下文，把目标章节任务卡、章节合同、Obsidian 待审软约束和项目学习版文风 / XP 放入资料分析提示；没有资料库或 Vault 正式命中时，也会把这份目标章节上下文返回给后续生成、改稿和复查动作。
- 章节生成、候选审校、章节核验和自动修订会共用章节连续性合同；合同把目标章节位置、人物状态、滚动摘要、蓝图锚点、近期章节尾段、叙事状态账本、剧情债务、人物弧线、Obsidian 约束和资料证据整理成同一份证据。生成提示词会把合同视为优先约束；章节核验有 `章节连续性合同` 维度，明确合同项缺失会计入自动修订触发条件，自动修订提示也会读取这份合同。章节核验还有 `项目记忆规则` 维度，会反查作者项目记忆里的“硬规则 / 警告”禁写表达，例如“不要提前揭示某人是主谋”“某人不能被提前揭示为主谋 / 真凶 / 卧底 / 潮师”“不要把 A 改名为 B”“铜钥匙不能被交给白石商会”“顾临不能死亡 / 叛变”或“林追不会主动暴露身份”，命中时记为 critical 并参与自动修订判断；正文写成“没有暴露身份”“并不是主谋”“并不是卧底”或“没有把铜钥匙交给白石商会”这类否定状态表述时不算违规；作者修改项目记忆后，已有章节核验会标为过期，刷新后按新规则重新检查。`chapter_generate` 和 `chapter_workflow/draft` 收到显式 `target_words > 0` 时按调用方字数目标生成；只有 `target_words=0` 的完整章生成才使用项目单章预算。
- 架构总览有“章节核验”页签，会展示每章核验分数、状态、维度、问题、建议和过期标记；项目记忆规则触发项会在该页签直接可见。
- 去 AI 改稿是小说正文专用处理：本地规则会检查通用模型腔、套版画面词、抽象情绪直说、潜台词解释、对白动作标签重复、句长 / 段长过齐和句首主语重复；prompt 会要求保留剧情事实、人物声音、伏笔和信息顺序，再处理句式、画面、节奏和结尾；完整章节改稿如果返回文本明显短于原文，会拒绝这次修订，避免把章节缩成摘要。
- Agent 自学习写作回归会自动运行去 AI A/B：固定小说片段对比原文和预期改写后的本地评分、平均提升、通过率、正文长度比例和残留问题，并扫描真实章节生成项目样本池，在自学习面板展示样本池和蒸馏规则；这组 A/B 不调用模型，用于持续监控规则退化和下一步强化方向。Agent 自学习模型审查会低频运行去 AI 裁判，回放历史 `chapter_humanize` 输出，把有效改法写入 `.gaoxia/learning/humanize_evolution_rules.json`，后续去 AI prompt 会读取这些项目规则；章节保存和做梦完成会排队 `humanize_review` 辅助任务，自学习心跳在排程开启且包含 `model_review` 时也会按样本签名变化、项目规则、历史输出分数和 12 小时冷却时间触发去 AI 智能巡检，巡检状态写入 `.gaoxia/learning/humanize_review_patrol.json`。
- Agent 章节生成、draft 写回或改稿写回后，如果当前保存生成或更新了 Obsidian 维护草稿，执行结果会追加 `obsidian_maintenance` 产物，列出相关待审草稿、状态和路径，让作者在同一个 Agent 结果里看到系统自动整理了哪些 Vault 候选资料；产物卡片可打开 Agent 自学习面板，并按来源章节和产物 `metadata.suggestion_ids` 定位对应维护项，避免长篇里章节号文本搜索误命中其它章节。
- Obsidian 待审草稿进入章节生成提示前会逐条读取真实草稿文件的 frontmatter 和正文小节，并校验维护 ID / 类型；待审草稿的 frontmatter 复用正式 Vault 笔记同一套 YAML 解析，作者手工改成 `required_beats: [{goal: ...}]`、`- {action: ...}`、`objective: >`、多行 `tags:` 或带行尾注释的写法时，章节提示、Agent 规划上下文和章节范围过滤仍能读取结构化字段与剧透边界；章节生成上下文和 Agent 规划上下文会按目标章节相关性排序待审草稿，并显示来源章节；目标章节可见的 `create_chapter_note`、`create_chapter_contract_note`、`create_style_rule_note` 和 `create_xp_rule_note` 还会单独进入“Obsidian 待审软约束”，作为低优先级补充提示，优先级始终低于作者明确要求和正式 Vault 设定；讨论入口的章节化 `brainstorm` 会复用这套章节安全提醒和软约束，不再只看手工拼接的项目摘要。
- Obsidian 关系字段支持 Markdown 内链，例如 `source_notes: "[当前线索](Clues/当前线索.md)"`、`related_characters:: [林追](../Characters/林追.md)` 和 `[林追](/Characters/林追.md)` 会按笔记路径、同目录相对路径或 Vault 根路径生成关系语义、可解析链接和反向链接；关系字段指向 `#小节` 或 `^block` 时，小节 / 块引用会进入“关系小节”上下文和知识检索，内部 links / backlinks 仍按笔记文件记录，未来笔记的小节名会按章节隐藏。
- Obsidian 关系字段里的普通字符串和列表项按逗号拆分多值时，会保留 Markdown 内链标签、双链别名和目标路径内部的逗号，例如 `source_notes: "[线索乙, 潮账](../Clues/线索乙.md)"`、`related_characters: ["[林追, 主角](../Characters/林追.md)"]` 和 `depends_on: "[[线索甲|账册, 初证]]"` 仍会生成关系语义、可解析链接和反向链接。
- Obsidian / Dataview 正文内联属性里的关系字段支持嵌套 Markdown 内链和双链别名逗号，例如 `[source_notes:: [当前线索, 潮标](当前线索.md)]`、`(related_characters:: [林追, 主角](林追.md))` 和 `depends_on:: [[线索甲|账册, 初证]]` 会生成关系语义、可解析链接和反向链接。
- Obsidian 可见 callout 里的任务列表会按正文解析，例如 `> - [ ] source_notes:: [[当前线索]]`、`> - [!] required_phrases:: 潮声异常`、`> ## 必须包含` 下的任务项，会进入内联属性、图谱关系、必写 / 禁写约束、章节范围和章节安全内容；`[!] / [?] / [>] / [/] / [-]` 这类常见 Obsidian Tasks 状态也会识别，隐藏 callout 仍会被排除。
- Obsidian Markdown 笔记、Canvas 文本节点和可见 callout 里的表格会提取可识别列名，例如 `章节目标 / 必须节拍 / 禁写动作 / 验收项 / 证据来源 / 相关人物` 会转成现有章节合同、写作约束和图谱关系行；单元格里的 `\|` 会还原为普通 `|`，表格里的双链、Markdown 内链和未来笔记仍走章节安全处理。
- Obsidian Markdown 内链目标里的英文括号会作为文件名内容解析，例如 `[林追旧档](../Characters/林追(旧).md)` 不会被提前截断；如果目标笔记对当前章节不可见，章节安全内容仍会隐藏链接和别名。
- Obsidian Markdown 内链目标里的转义字符和 title 会按链接语义处理，例如 `[林追旧档](../Characters/林追\(旧\).md "旧档")` 会还原为 `Characters/林追(旧).md`，`[终局答案](../Secrets/未来真相 "终局")` 会去掉 title 后解析；未来笔记仍按目标章节隐藏。
- Obsidian Markdown 引用式链接会进入图谱解析，例如 `[林追旧档][old]`、`[林追旧档]`、`[old]: ../Characters/林追\(旧\).md "旧档"` 和 `[林追旧档]: ../Characters/林追\(旧\).md "旧档"` 会生成可解析链接、反向链接和关系语义；引用定义指向未来笔记时，路径和 title 仍按目标章节隐藏；Markdown 脚注 `[^id]: ...`、缩进续行和正文脚注标记不作为知识图谱链接解析，也不会进入 AI 可见正文。
- Obsidian HTML `<a href="...">` 链接会按 Markdown 内链进入图谱解析；目标是 Markdown 或 Canvas 笔记时会生成可解析链接、反向链接和关系语义，目标是本地附件时按附件过滤，目标是未来笔记时按章节安全内容隐藏链接标签和路径。
- Obsidian URI 链接会按 Vault 内部链接解析，例如 `obsidian://open?vault=Demo&file=Characters%2F林追.md` 和 `obsidian://advanced-uri?vault=Demo&filepath=Secrets%2F未来真相.md&heading=终局答案` 会生成可解析链接、反向链接和关系语义；`file / filepath / filename / path` 作为 Vault 根路径目标，`heading / header / section / block / blockid / block_id` 或 URI fragment 会保留为小节 / 块引用；frontmatter 关系字段、Markdown 链接、HTML `<a>` 链接和 Canvas link 节点里的 URI 指向未来笔记时，章节安全内容仍会隐藏链接标签、路径和关系语义。
- Obsidian 相对路径双链会按来源笔记路径和路径段解析，例如 `[[../Characters/林追]]`、`[[./当前线索]]`、`[[Clues/../Characters/林追]]` 和 `[[/Characters/林追]]` 会生成可解析链接、反向链接和关系语义；指向未来笔记时，章节安全内容、检索预览和证据正文仍会隐藏未来标题与别名。
- Obsidian URL 编码双链会先解码再解析，例如 `[[Characters/林%20追]]` 和 `[[../Secrets/未来%20真相]]` 会生成可解析链接、反向链接和关系语义；指向未来笔记时，章节安全内容、检索预览和证据正文仍会隐藏未来标题、路径与别名。
- Obsidian URL 编码保留字符会按路径内容处理，例如 `[[Characters/林%23追]]` 和 `[线索](Secrets/未来%5E真相.md)` 不会被误拆成 heading、query 或 block 引用；章节安全内容仍按解码后的真实目标隐藏未来笔记。
- Obsidian 越出 Vault 的相对链接会被排除，例如根目录笔记里的 `[[../Outside/旧设定]]`、`[[Clues/../../Outside/旧设定]]`、`[[/../Outside/旧设定]]` 或 `[外部](/../Outside/旧设定.md)` 不会生成未解析链接、图谱关系或维护建议；子目录内回到 Vault 内部的相对路径仍正常解析。
- Obsidian 块引用会按笔记级链接解析，例如 `[[当前线索^block-id]]` 和 `[[当前线索#小节^block-id]]` 会生成可解析链接、反向链接和关系语义；如果目标笔记对当前章节不可见，章节安全内容仍会隐藏未来标题。
- Obsidian 同笔记内部链接不会生成跨笔记图谱关系，例如 `[[当前线索#内部索引]]`、`[[当前线索^scene-a]]` 和 `[回看](当前线索.md#内部索引)` 指向当前笔记时，会从 links、resolved_links、backlinks 和 graph_relations 中排除，避免目录、heading 和 block 导航污染图谱统计。
- 章节安全记录会保留不涉及未来笔记的未解析双链和可见范围内的歧义双链，让章节上下文继续显示需要整理的图谱风险；歧义名称如果同时命中目标章节不可见的笔记，仍会按章节安全规则隐藏。
- Obsidian 正文关系小节会参与图谱解析：`## 来源笔记`、`## 相关人物`、`## 伏笔`、`## 兑现` 等小节列表，以及 `相关地点：[[旧码头]]` 这类正文行，会生成图谱关系、可解析链接和反向链接，并按目标章节隐藏未来关系目标。
- Obsidian Markdown 和 Canvas 文本里的 `%%...%%` 注释、Markdown HTML 注释 `<!-- ... -->`、fenced code block、inline code、Markdown 删除线、HTML 删除标签和隐藏 HTML details 会从 AI 可见正文中排除，不参与正文内联属性、双链、标签、关系小节、必写 / 禁写、章节范围、检索预览和章节上下文解析。
- Obsidian Markdown 和 Canvas 文本里的隐藏 callout 会从 AI 可见正文中排除；`spoiler / future / private / hidden / draft / todo / no-ai` 以及 `剧透 / 未来 / 隐藏 / 私密 / 草稿 / 待定 / 勿用 / 不引用` 类型不参与正文内联属性、双链、标签、关系小节、必写 / 禁写、检索预览和章节上下文解析，同一引用块内后续普通 callout 仍视为隐藏内容，结束引用块后重新开始的 `note / info` callout 才按正文处理；普通 callout 里嵌套的隐藏 callout 只排除嵌套块，外层公开内容仍按正文处理。
- Obsidian Markdown 和 Canvas 文本里的隐藏 HTML details 会从 AI 可见正文中排除；`<details>` 的 `summary` 或 `class` 等属性带 `spoiler / future / private / hidden / draft / todo / no-ai`，或带 `剧透 / 未来 / 隐藏 / 私密 / 草稿 / 待定 / 勿用 / 不引用` 时，整段折叠内容不参与正文内联属性、双链、标签、关系小节、必写 / 禁写、检索预览和章节上下文解析；折叠正文里写 `no_ai:: true`、`AI不可用:: 是`、`#no-ai`、`#AI不可用`、`draft:: true`、`private:: true`、`archived:: true` 或过滤状态时，也只排除该折叠块；普通公开 details 仍按正文处理。
- Obsidian `![[...]]` 嵌入只在目标是 Markdown 或 Canvas 笔记时进入图谱，并写入 `embedded_links`；章节上下文会把目标章节可见的嵌入笔记显示为短预览。图片、PDF、音频等附件嵌入、Markdown 图片、HTML 媒体标签和指向本地非笔记附件的 Markdown 链接 / 引用式链接会从 AI 可见正文与图谱关系中排除；未来笔记嵌入不会在早期章节预览里显示。
- Markdown 笔记和 Canvas 里的 HTTP(S) 考据入口会写入 `external_links`，Markdown 链接标题、HTML `<a>` 文本、Canvas link 标签和结构化来源名会写入 `external_references`：支持 `source_url / reference_links / research_links / external_links / references / sources / citations / url / 资料链接 / 资料来源 / 参考链接 / 参考资料 / 考据链接 / 考据来源` 等 Properties、正文内联属性、Markdown 外部链接、引用式链接定义、裸 URL、HTML `<a>` 和 Canvas link 节点 URL；同步面板会统计考据链接数量，技能库和架构总览的 Obsidian 笔记卡片、章节上下文会优先显示“考据来源”。外部 URL 不会写入 Vault 内部 `links / backlinks / graph_relations`，也不会生成未解析链接维护建议。
- Obsidian 考据来源会继续进入长篇生产链路。目标章节可见笔记里的 `external_references` 会写入叙事状态账本章节任务卡，出现在模型叙事编辑提示、Agent 路由 / 规划能力上下文、自学习面板最新章节任务卡和任务蒸馏材料备注中；项目蒸馏签名也会记录 `external_references / external_links`，考据来源变化后会刷新对应蒸馏资料，让章节计划、资料考据和写作执行使用同一批来源说明。
- Canvas 文本节点可用 `title / 标题 / name / 名称` 声明 Canvas 笔记标题，覆盖画布文件名并进入同步摘要、知识检索和章节上下文；没有这些字段时仍使用文件名。
- Canvas link 节点的 HTTP(S) URL 会进入 Canvas 正文、知识检索和章节上下文，作为外部考据入口保留；外部 URL 不会写入 Vault 内部 links / backlinks，也不会生成未解析链接维护建议。Canvas link 节点的 `obsidian://open` / `obsidian://advanced-uri` URL 会按 Vault 内部链接解析，进入可解析链接、反向链接和图谱关系；没有 `label / text` 的内部 URI link 节点会用目标笔记名作为 Canvas 边关系标签；指向未来笔记时，章节安全正文会隐藏链接标签、目标路径和关系语义。
- Canvas file 节点支持按 Canvas 所在路径解析相对目标，例如 `../Clues/当前线索.md` 会生成可解析链接、反向链接和关系语义；file 节点的 `subpath` 或文件路径里的 `#小节` 会进入 Canvas 正文和章节安全上下文，但内部 links / backlinks 仍按笔记文件记录，不把小节当成新笔记；指向未来笔记时，章节安全正文仍会隐藏未来节点、小节和边标签。
- Canvas group 分组节点会按画布坐标识别内部节点，分组标题和分组内 file 节点关系会进入图谱关系、知识检索和章节上下文；分组指向未来笔记或分组标题提到未来笔记名时，目标章节安全内容仍会隐藏。带 `no_ai:: true`、`#no-ai`、`AI不可用:: 是`、`draft:: true` 或过滤状态的 Canvas 文本 / group 节点会按节点排除，隐藏 group 内部节点也会排除；`no_ai:: false` 这类明确可用声明不会误删公开节点；同一张 Canvas 的公开节点仍正常同步。
- Canvas 边如果指向目标章节不可见的已知笔记，章节安全正文会把该边标签改为“未开放关系”；如果边指向当前可见笔记，但边标签或图谱关系标签写到未来笔记名，也会按章节安全内容改写，避免关系图边标签提前透露后段设定。
- 架构总览的关系、事件和世界要素只读取 `.gaoxia/story_overview_model.json` 的模型版全书总览缓存；模型总览不可用、生成失败或结果没有通过证据校验时，项目详情会返回模型总览状态和错误，不会从本地架构文件抽取结构化节点。手动打开架构总览或刷新模型总览会直接再次请求模型，不返回 `retry_after` 失败倒计时。章节生成、改稿、诊断上下文和项目级文风 / XP 提示不会读取模型总览缓存，项目记忆和续写 / 改稿类项目蒸馏包也不会从模型总览里的全书实体反写；没有目标章节时，续写、改稿、仿写和人物任务默认不带入 Obsidian 后段笔记，整书架构任务仍可使用全书资料，避免后段 Obsidian 设定通过总览缓存或蒸馏报告进入早期章节。
- 章节保存后会维护项目内 `.gaoxia/learning/style_xp_evolution.json` 和 `.gaoxia/learning/narrative_state.json`，用于后续章节上下文；章节保存接口会在响应 `meta.self_evolution` 带回最新自学习状态，技能库保存生成章或改稿章后会立即刷新自学习面板里的章节任务卡和 Obsidian 维护摘要；技能库写回章节时会透传当前 XP 预设，让 `.gaoxia/learning/style_xp_evolution.json` 记录作者实际选择；叙事状态账本的章节任务卡会携带目标章节可见的 Obsidian 约束，并记录已满足必写项、未完成必写项和已触犯禁写项；未完成或触犯的 Obsidian 要求会转为后续章节可见的高优先级叙事债务，修订满足后关闭，模型叙事编辑会读取当前章和下一章的 Obsidian 约束来生成章节合同；启用 Obsidian 时，账本会为未入 Vault 的剧情债务、人物弧线和图谱问题生成维护建议，包含建议笔记路径、处理动作，以及带来源 ID、来源章节、相关人物字段、人物双链或来源笔记路径的 Markdown 草稿；剧情债务和人物草稿会按来源章节写入 `reveal_after_chapter`，多个来源章节时按最晚来源章开放，剧情债务草稿同时写入 `debt_content / debt_status / risk_level / next_required_action`，人物状态草稿写入 `character / phase / current_state / unresolved_pressure / required_next_check` 并带 `人物状态` 标签，发布后早期章节不会读到后段自动维护笔记；账本会生成 `obsidian_maintenance_summary` 维护摘要进入 Agent 规划上下文；有目标章节时，Agent 会按目标章节可见的维护项重新计算维护摘要和建议明细，避免第 2 章被第 60 章维护压力影响；没有明确目标章节的非架构任务只保留全局维护摘要，不暴露后段建议标题、路径或动作；Agent 路由 / 规划和自学习状态读取当前项目详情时会刷新这些维护建议，并可自动写入中高优先级待审草稿；维护队列按 80 章长篇规模保留章节档案建议和操作记录，一次自动保存更多中高优先级草稿；重复出现的未解析双链会形成 `Graph/` 待审草稿并继承来源笔记的章节范围和剧透边界，来源章节范围不连续时会改用较晚可见的剧透边界，多个开放式来源起点不同时也按较晚来源生成剧透边界，避免生成过宽章节范围，重名和歧义链接会形成修复提醒；已解析双链如果来源笔记可见范围没有被目标笔记可见范围覆盖，会形成章节范围不匹配风险；带未解析或歧义双链的笔记不会被计入孤立笔记；中高优先级建议会在章节保存、Obsidian 同步或章节上下文生成时自动写入项目 `.gaoxia/obsidian_drafts/` 待审草稿；已有自动草稿未被人工改动时，后续图谱来源列表、来源内容或章节边界变化会更新草稿，人工改动过的草稿和保存草稿时遇到的既有人工内容都不会被自动覆盖；同一路径的维护建议即使因来源变化产生新 ID，也会沿用已有草稿状态；作者可忽略暂不处理的维护建议，同一路径新建议会继承忽略状态并跳过自动草稿，也可恢复处理让后续同路径建议重新进入待处理和自动草稿流程；草稿文件被移动或删除时会显示草稿缺失，作者可重新保存；发布过的 Vault 笔记被移动或改名后，系统会先按内容签名识别；新生成的维护笔记带 `gaoxia_maintenance_id`，发布到 Vault 前也会恢复缺失的身份字段，内容被作者改过但身份唯一匹配时也会把维护记录改到新的 Vault 路径；如果对应维护项仍在列表中，面板会显示移动前后路径；没有唯一匹配或文件被删除时会显示 Vault 笔记缺失，作者可重新发布；Vault 待更新项保存新版草稿时会额外生成合并草稿，合并草稿只放在项目 `.gaoxia/obsidian_drafts/`，标出正式 Vault 笔记和系统建议路径，不覆盖 Vault 正式笔记；作者在 Obsidian 完成合并后可在面板单条或批量确认，系统记录当前 Vault 内容并刷新 Obsidian 摘要和知识库；自学习面板会显示完整维护列表和维护摘要，支持按状态筛选并按标题、路径、章节或动作搜索，也支持把当前筛选结果批量保存到项目 `.gaoxia/obsidian_drafts/`、把当前筛选结果里已保存的草稿显式批量发布到 Vault、批量确认当前筛选结果里的 Vault 合并项、批量忽略当前筛选结果里暂不处理的维护建议，或批量恢复当前筛选结果里的已忽略建议；面板会显示自动草稿、人工改动草稿、保留人工草稿、草稿缺失、Vault 笔记缺失、Vault 笔记已移动、Vault 待更新、Vault 合并草稿、确认 Vault 已合并、已忽略、恢复处理和批量恢复入口；用户显式发布时写入配置的 Vault，发布会检查目标路径在 Vault 内且不覆盖已有笔记，并重新同步 Obsidian 摘要和知识库，让新笔记进入图谱解析。
- Agent 自学习面板的 Obsidian 维护摘要在没有筛选时显示后端全局摘要；存在状态、来源章节、搜索或产物 ID 筛选时，会按面板可见维护项重新统计待处理、高优先级、自动草稿和状态数量。Agent 结果里的 `obsidian_maintenance` 产物打开第 N 章维护队列后，会按来源章节和产物 `metadata.suggestion_ids` 定位对应维护项，摘要总数与可见列表一致，不显示全书统计。
- 自学习面板保存 Obsidian 配置或手动同步 Vault 后，接口会随作品详情返回最新自学习状态，让新增 Vault 笔记触发的图谱风险、维护摘要和章节任务卡立即更新；执行 Obsidian 维护保存、发布、确认合并、忽略或恢复后，维护接口也会返回最新自学习状态，前端优先用响应里的状态更新维护摘要和章节任务卡，并重新读取作品详情；发布 / 确认合并还会刷新 Obsidian 同步状态。若自学习状态或作品详情刷新失败，界面保留维护成功提示并显示失败原因。
- 自学习候选状态、草案状态、草案应用、技能维护、写作回归、模型审查、排程设置和排程执行接口会在响应 `meta.self_evolution` 带回最新自学习状态；前端优先使用响应状态更新候选、草案、趋势、失败案例、Obsidian 维护摘要和章节任务卡，响应缺少状态时才重新读取自学习状态。
- 已保存章节如果没有匹配到可用 Obsidian 章节档案，叙事状态账本会生成 `ChapterNotes/` 待审草稿；草稿使用 `type: chapter_note`、来源章节、`chapter_index / chapter_title / chapter_summary / handoff_to_next / chapter_excerpt / obsidian_required_satisfied / obsidian_required_missing / obsidian_forbidden_violations`、`source_ids`、`source_chapters`、`source_chapter_hash`、`source_notes`、`related_locations / related_props / related_organizations` 和 `reveal_after_chapter` 描述来源正文版本、章节回顾、下一章交接、Obsidian 执行状态、来源笔记、图谱关系与可见边界，`source_notes` 会包含本章命中的 Vault 笔记、章节计划、剧情债务和人物弧线来源，但不引用其它章节档案；正文会列出本章 Obsidian 章节计划、剧情债务、人物弧线和下一章交接，显式发布后才写入 Vault，并只对后续章节检索可见；未发布前，目标章节可见的 `create_chapter_note` 待审草稿会在章节上下文和 Agent 规划上下文显示短预览，包含章节摘要、下一章交接和未完成 Obsidian 必写项；作者删掉 frontmatter 但保留“章节摘要 / 下一章交接 / 未完成的 Obsidian 必写项 / 章节正文摘录”正文小节时，短预览会从正文小节读取，并继续标明不能当作 Vault 正式设定引用。
- 未被人工改动的自动章节档案草稿会随章节正文变化刷新；章节标题变化时迁移到新的 `ChapterNotes/` 文件名并移除旧自动草稿。人工改动过的章节档案草稿保留原文件和状态，不会被自动覆盖。自动发布过的章节档案在章节正文或标题变化后会进入 Vault 待更新状态，且不会把已发布的章节档案当成本章来源笔记引用自己。同步器会读取 `source_chapter_hash`，即使本地维护动作记录缺失，也能识别由系统生成但对应旧正文的章节档案；发布后的章节档案如果保留 `source_ids`、`source_chapters`、`章节来源 / 来源章` 或正文“来源章节 / 来源章”，作者改名、移动或重写标题后，系统仍会按来源章节控制可见范围，把来源章节保存在 `ObsidianNoteSummary`、知识内容头部、技能库 Obsidian 知识库卡片和架构总览 Obsidian 卡片，并识别为同一章档案；`source_ids` 里的章节来源 ID 支持 `chapter-058 / chap-058 / ch058 / Chapter 58`，只有整段 ID 是章节格式时才会推断来源章节；作者把类型改成 `author_archive` 并删除 `source_ids` 时，只要保留 `source_chapters` 或中文来源章节别名，维护队列仍按同一章档案处理；同一档案带多个章节来源 ID 或来源章节时，以最晚来源章节作为开放起点；文件名、路径和来源章节冲突时按更晚的开放章节处理；如果 `source_ids` 或 `source_chapters` 与 `第058章-回顾.md` 这类单章文件名指向同一章，来源标记保持后续可见，不被文件名限制成单章可见。
- 两篇以上没有正文双链、frontmatter 关系、未解析或歧义双链和反向链接的正式 Obsidian 笔记会生成 `Graph/孤立笔记整理-{来源摘要}.md` 待审索引草稿；发布后索引笔记通过 `source_notes` 和正文双链连接这些笔记，让它们进入 backlinks 和后续图谱检索；后续新增孤立笔记会得到新的来源摘要路径，不会继承旧索引的已发布状态。
- 剧情债务草稿会把预计处理区间写成 `expected_payoff_range`，不写成 `chapter_range`；`chapter_range` 只表示 Obsidian 笔记可见范围，不能用来表达债务计划区间。
- 章节生成上下文和 Agent 规划上下文会按目标章节显示 Obsidian 待审草稿提醒，并把来源章节最贴近目标章的草稿排在前面；带章节范围或剧透边界的草稿（包括作者手工改动后的草稿文件）不会进入范围外章节提示，草稿 frontmatter 字段名也支持常见属性写法，并且提示会标明不能当作 Vault 正式设定引用；`create_chapter_note` 草稿会额外显示章节档案短预览，`create_chapter_contract_note` 草稿会额外显示合同目标、必须节拍、禁写动作和验收项短预览，`create_style_rule_note` / `create_xp_rule_note` 草稿会额外显示规则、适用范围、检查项、证据数和置信度短预览，其它草稿类型不会被误当作章节档案、章节合同或文风 / XP 规则；草稿或维护建议只保留 `source_chapters` / 正文“来源章节”、没有显式 `chapter_range` 或 `reveal_after_chapter` 时，Agent 上下文按最晚来源章节开放，避免早期章节看到含后段来源的维护项；Graph 待审草稿来自多个开放起点不同的来源笔记时，也按较晚来源写入剧透边界。
- 草稿文件被移动或删除后，章节生成上下文仍会用维护项里的 `source_chapters` / 正文“来源章节”判断可见范围；含后段来源的缺失草稿不会提前进入早期章节提示。
- 作者手工改动待审草稿并删除 frontmatter 时，章节生成上下文仍会读取正文里的 `source_chapters::` 或“来源章节：...”来判断可见范围；如果这是 `create_chapter_note` 章节档案草稿，正文里的章节摘要、下一章交接、未完成 Obsidian 必写项和章节正文摘录也会继续进入短预览。
- 章节化证据检索在替换为目标章节安全内容后，会再次确认查询词仍存在；只命中未来双链、Markdown 内链、未来标题或已知未来笔记纯文本标签的 Obsidian 候选不会进入连续性证据包。
- 架构总览数据来自后端统一构建；模型版总览缓存保存在 `.gaoxia/story_overview_model.json`，结构化节点不使用本地架构文件抽取结果。
