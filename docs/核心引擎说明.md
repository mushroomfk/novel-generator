# 核心引擎说明

本文记录当前核心引擎的三条主线：

1. `Embedding / RAG`
2. `分步架构引擎`
3. `文风 DNA 校准`

这些能力都已经接进当前界面，不额外开新页面，入口都在现有的技能区和设置区。

## 1. Embedding / RAG

### 现在的结构

- 写作模型和 Embedding 模型分开配置；设置页默认按当前写作模型推导 Embedding，用户也可以勾选“单独设置 Embedding”保存独立配置。
- 设置面板现在会同时保存两组配置：
  - `model`：章节生成、蓝图、架构、文风分析这些文本生成任务
  - `embedding`：知识切片的索引和查询向量
- 后端配置文件现在是统一的 `AppConfig`，内部包含 `model` 和 `embedding` 两块。
- 后端只有收到旧的单独 `ModelConfig` 保存请求时才自动推导 Embedding；收到完整 `AppConfigUpdateRequest` 时会保留传入的 `embedding`，避免覆盖用户单独填写的向量模型。

### 索引方式

- 项目创建后，后端会把这些内容纳入知识索引：
  - 架构文件
  - 章节正文
  - 用户导入的资料
- 知识库底层现在分成三层：
  - `knowledge_chunks`：原始切片
  - `knowledge_chunks_fts`：SQLite FTS 关键词检索
  - `knowledge_vectors`：二进制向量、维度和范数
- 每次重建索引时，后端会记录：
  - `source_signature`
  - `embedding_signature`
- 章节、设定文件、导入资料发生变化时，会按 `source_key` 刷新对应来源的切片和向量。
- 只有目标章节数、schema、来源签名或 Embedding 配置发生整体变化时，才会重建完整索引。
- 旧版 `vector_json` 行会在初始化知识库时迁移到二进制向量列。

### 查询方式

- `search_project_knowledge()` 现在是混合检索：
  - 先用 FTS 取候选
  - 再只对候选做语义相似度计算
  - 阿里百炼配置可额外调用 `qwen3-rerank` 官方 OpenAI 兼容端点 `/compatible-api/v1/reranks`
  - 最后合并排序
- 返回结果会带 `match_type`：
  - `keyword`
  - `semantic`
  - `hybrid`
  - `rerank`

### 目前支持

- OpenAI-compatible `/embeddings`
- 阿里百炼 `qwen3-rerank` 官方 OpenAI 兼容 `/compatible-api/v1/reranks`
- 动态配置 `base_url`、`model_name`、`dimensions`、`retrieval_k`、`batch_size`
- 技能区里直接导入资料、搜索资料，并把结果用于项目上下文
- 项目资料库和作者参考库支持 `txt / md / json / csv / html / docx / pdf` 文件导入
- `联网考据` 使用国内博查搜索查询历史典故和史实，结果可保存为项目资料继续参与检索

### 目前边界

- 现在还是项目内混合检索，不是独立的外部向量库服务。
- 搜索索引仍保存在项目本地 SQLite，不依赖独立外部向量库。

## 2. 分步架构引擎

### 目标

整本架构不再只是一键生成四段文本，现在可以按模块逐步生成、逐步修改、逐步写回。

### 当前步骤

技能区 `分步架构` 支持这些模块：

- `core_seed`
- `character_design`
- `world_building`
- `plot_structure`
- `character_state`
- `blueprint`
- `global_summary`

### 工作方式

- 前端先把当前项目文档同步到一个 `workspace`
- 用户选择要生成的步骤
- 后端只生成这个步骤对应的内容
- 返回：
  - `headline`
  - `summary`
  - `content`
  - `checklist`
- 如果模型把架构段落返回成嵌套 JSON、数组或对象，后端会先转成可写入设定文件的文本，避免把外层响应结构直接存入项目文档。
- 前端可以只替换当前模块，也可以一次性把整个工作区写回项目
- 架构总览读取项目设定时，会优先使用大模型整理后的结构化总览；人物状态、关系、事件、地点、道具、技能和组织都必须带来源证据，证据校验通过后才进入总览。项目来源较长时会分片交给模型整理，再合并通过证据校验的节点。本地文本抽取只在没有可用模型总览时作为补充。
- 模型总览会按项目来源签名缓存在 `.gaoxia/story_overview_model.json`，设定、资料或章节变化后签名失效，再重新整理。

### 继续扩写

- `mode=continue` 时，分步架构会带上新增章节目标
- 写回项目时可以直接同步更新 `target_chapters`

### 使用位置

- 技能区 `分步架构`
- 后端接口：
  - `POST /api/generate/architecture/step/stream`
  - `PUT /api/projects/{project_id}/architecture/workspace`

## 3. 文风 DNA 校准

### 现在有的能力

- 样文分析
- DNA 分析
- 文风融合
- 文风校准
- 叙事校准
- 校准回滚
- 作者参考库
- 作者参考库语义检索

### 数据结构

每个文风方案现在会保存这些核心字段：

- `instruction`
- `analysis`
- `dna_analysis`
- `narrative_for_architecture`
- `narrative_for_blueprint`
- `narrative_for_chapter`
- `source_sample`
- `calibration_reference`
- `calibration_notes`
- `last_calibrated_at`
- `pre_calibration_snapshot`

### 三段式叙事约束

文风方案现在不只管“语言像不像”，还会分别约束：

- 架构阶段怎么写
- 蓝图阶段怎么写
- 正文阶段怎么写

这三段内容会直接被生成链路读取。

### 作者参考库检索

- 作者参考资料现在不只是列表展示。
- 技能区里可以直接按关键词或语义搜作者参考。
- 后端会给作者参考资料补向量，查询时做混合排序。
- 文风方案在参与章节、蓝图、架构生成时，会优先带上命中的作者参考片段。

### 校准机制

- 第一次校准前，会自动保存 `pre_calibration_snapshot`
- `校准文风` 主要收敛：
  - `instruction`
  - `analysis`
  - `dna_analysis`
- `校准叙事` 主要收敛：
  - `narrative_for_architecture`
  - `narrative_for_blueprint`
  - `narrative_for_chapter`
- 回滚时会恢复快照里的文风字段和校准备忘

### 使用位置

- 技能区 `文风与DNA`
- 后端接口：
  - `POST /api/studio/styles/analyze/stream`
  - `POST /api/studio/styles/analyze-dna/stream`
  - `POST /api/studio/styles/calibrate/stream`
  - `POST /api/studio/styles/calibrate-narrative/stream`
  - `POST /api/studio/styles/{name}/rollback-calibration`

## 4. 上下文预算与模型错误分类

### 上下文预算

- `build_project_context_bundle()` 会按任务类型计算上下文预算，返回 `budget_report`。
- 当前章节正文过长时，会保留开头和结尾，中间用说明性占位文本替代。
- 检索线索过长时，会优先压缩检索块，避免资料片段挤掉任务指令和章节上下文。
- 当前默认字符预算：
  - 架构、续写：`18000`
  - 人物视角：`16000`
  - 文风模仿：`20000`
  - 改写模式：`22000`

### 模型错误分类

- 生成链路和做梦链路现在会把模型调用异常分类后写入 `prompt_history.jsonl`。
- 已分类字段包括 `error_kind`、`error_title`、`error_user_action`、`error_retryable`。
- 当前覆盖认证、账单、限流、上下文过长、模型不存在、网络连接中断、服务端错误、超时、请求格式错误和未知错误。
- 主模型、Embedding 和重排序请求共用 `request_json_with_retries()`；`SSL: UNEXPECTED_EOF_WHILE_READING`、`Remote end closed connection`、`connection reset` 等连接中断会短重试，HTTP 认证、额度、模型名和请求格式类错误不会重试。
- `Prompt 历史` 查询会同时匹配错误类型和错误标题，便于从日志里定位模型配置或服务状态问题。

## 5. 这些模块和现有界面的关系

- 设置区：
  - 新增 Embedding 配置
- 技能区：
  - `知识检索` 现在可以走语义检索
  - `联网考据` 可以把国内联网搜索结果整理成写作素材，并手动写入资料库
  - `分步架构` 现在是真正的工作区，不是占位
- `文风与DNA` 现在能直接展示、保存、校准、回滚
- `文风与DNA` 里还能直接搜索作者参考库

外层布局没有改，还是现有界面。

## 6. 回归结果

当前实际做过的验证：

- `npm run verify`
  - 后端 108 个用例通过
  - 前端生产构建通过
- `npm run verify:ui`
  - 浏览器 smoke 通过，覆盖 Agent 章节计划、混合命令、整书架构、讨论结果、提示词方案、XP 预设、文件浏览和人物复刻

## 7. 后续可继续做的事

现在这三块已经能用。后面如果还要继续往参考项目靠，可以继续补这些：

- 外部知识源解析：PDF、DOCX、网页、长文批量导入
- 更细的架构分步缓存和取消机制
- 更细的文风参考片段分组和权重策略
- 更长链路的自动写作编排，把架构、蓝图、正文、定稿串成批处理任务
