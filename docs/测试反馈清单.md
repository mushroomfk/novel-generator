# 稿匣测试反馈清单

## 基本信息

- macOS 版本：
- 设备型号：
- 芯片类型：`Apple Silicon / Intel`
- 安装来源：`dmg`

## 安装过程

- [ ] 能挂载 dmg
- [ ] 能把 app 拖到“应用程序”
- [ ] 第一次打开能按说明完成放行
- [ ] 应用能正常进入主界面
- [ ] 能复制当前设备码
- [ ] 能导入测试许可证，许可证状态显示“已激活”

## 核心功能

- [ ] 能创建作品
- [ ] 能在左侧作品列表重命名作品
- [ ] 能在左侧作品列表打开作品目录
- [ ] 能在左侧作品列表删除作品
- [ ] 能从作品菜单导出项目迁移包，并在作品 `exports/` 目录看到 `.gaoxia-project.zip`
- [ ] 能从工作台右上角“更多”菜单导入项目迁移包，并确认新作品保留章节、设定、普通知识库、`.gaoxia` 状态和本地历史；外部 Obsidian Vault 的索引正文、笔记摘要、预览、维护队列、维护草稿、蒸馏摘要、Agent 线程资料分析记录和 workflow run 资料分析摘要不会随迁移包保留，线程上下文索引导入后可重新生成
- [ ] 能打开章节
- [ ] 能保存正文
- [ ] 能打开技能库
- [ ] 能确认技能目录里的内置技能带有 Agent action、mode 和确认要求：`续写正文` 对应 `chapter_workflow/draft`，`去 AI` 对应 `rewrite_chapter/humanize`，`一致性检查` 对应 `consistency_check`
- [ ] 能确认主对话快捷按钮会按内置技能元数据生成 Agent 计划：点“续写本章”后计划里出现 `chapter_workflow` 且 `mode=draft`，点“判断本章”后计划里出现 `chapter_workflow` 且 `mode=diagnose`
- [ ] 能跑知识检索
- [ ] 能配置 Obsidian Vault，并确认笔记、适用章节、必须包含 / 禁止出现短语、双链和 Markdown 内链的已解析链接、反向链接、未解析链接、重复命名统计和结构化问题项进入知识检索与技能面板
- [ ] 能确认 `第58章-线索.md`、`第59-60章-后续.md`、`Chapters/58/设定.md`、`chapter-61.canvas` 这类 Obsidian 文件名或路径会被识别为章节范围，并按目标章节过滤
- [ ] 能确认 `#第58～60章`、`#Ch58～60`、`第58～60章-计划.md` 和 `ch58～60-plan.md` 这类带中文全角波浪线的章节范围标签或文件名，会按第 58 到 60 章过滤，范围外章节不可见
- [ ] 能确认 Obsidian Canvas 默认路径规则包含 `**/*.canvas`；Canvas 文本节点里的 `title / 标题 / name / 名称` 会作为笔记标题进入同步摘要、知识检索和章节安全上下文；Canvas link 节点的 HTTP(S) URL 会进入知识检索和章节安全上下文，但不会写入 Vault 内部 links / backlinks 或未解析链接维护建议；Canvas link 节点的 Obsidian URI 会作为 Vault 内部链接进入 links、backlinks、图谱关系和章节安全上下文；无标题内部 URI link 节点作为 Canvas 边端点时会显示目标笔记名；Canvas file 节点和边会进入图谱关系、反向链接和章节安全上下文；Canvas group 分组会按画布坐标识别内部节点，分组标题和分组内 file 节点关系会进入图谱关系和章节安全上下文；Canvas file 节点关系进入章节上下文时会显示可见笔记标题，而不是原始文件路径；单个 `no_ai:: true` 文本节点不会让整张 Canvas 失效，隐藏节点和隐藏 group 内部节点不会进入图谱、检索、约束或反向链接，`no_ai:: false` 文本节点仍会作为公开节点同步
- [ ] 能确认 Canvas file 节点的 `subpath` 或文件路径里的 `#小节` 会进入章节安全上下文，内部 links / backlinks 仍按笔记文件记录，指向未来笔记的小节不会在早期章节检索里返回
- [ ] 能确认 Obsidian frontmatter 字段名使用大小写、空格、连字符或下划线等常见属性写法时，状态、摘要、关键词、别名、标签、章节范围、剧透边界和必写 / 禁写项仍能被识别
- [ ] 能确认 `aliases: ["潮师, 守账人"]`、`keywords: ["旧船队, 暗账"]`、`required_phrases: ["潮声异常, 不得提前解释"]` 这类 Obsidian frontmatter 数组不会把引号里的逗号误拆成多项；`aliases: 潮师；守账人`、`source_notes: 当前线索；未建笔记`、`required_phrases: 潮声异常；不得提前解释` 这类普通列表型 Properties 会按半角或中文分号分成多项
- [ ] 能确认 `summary: >`、`description: |`、`summary: >2-`、`keywords: |2-` 这类 Obsidian frontmatter 多行 Properties 会进入知识索引和章节上下文；多行 `keywords / source_notes / required_phrases` 会进入检索词、图谱关系和写作约束；这些列表字段的单项写成 `- >` 或 `- |` 时也会读取真实多行正文
- [ ] 能确认没有显式 `type / kind / 类型` 的 Obsidian 笔记放在 `Characters / Locations / Plans / Debts / CharacterArcs / Style / XP` 等常见目录时，会显示对应类型；作者显式写 type 时不会被目录覆盖
- [ ] 能确认没有显式 `type / kind / 类型` 的 Obsidian 笔记只写 `#人物/主角`、正文 `#人物／主角`、`#章节计划/58`、`#剧情债务/伏笔` 这类层级标签时，会显示对应类型；`#非人物/主题` 不会被误判成人物
- [ ] 能确认章节计划类 Obsidian 层级标签会同时绑定章节范围：`#章节计划/58`、`#章节合同/58-60`、`#场景卡/59`、`#scene-plan/59` 不写 `chapter_range` 时，也只在对应章节进入知识检索和章节上下文
- [ ] 能确认 Obsidian 多选 Properties 写成 `type: [主角, 人物]`、`type: [临时, 章节计划]` 时，会扫描整个列表并显示对应类型；`type: [自定义分类]` 会保留作者写法且不被目录覆盖
- [ ] 能确认 Obsidian 路径过滤按大小写不敏感匹配：默认 `.obsidian/**`、`.trash/**`、`templates/**` 能排除 `.OBSIDIAN/`、`.Trash/`、`Templates/`，自定义 `drafts/**` 能排除 `Drafts/`
- [ ] 能确认 Obsidian 候选笔记超过 `max_notes` 时，会按 Vault 相对路径稳定排序后再取前 N 篇，并在同步警告里提示有候选笔记未同步；继续增删排在上限后的候选笔记时，同步摘要里的 skipped 和警告会随之更新
- [ ] 能确认 Obsidian AI 可见性属性和标签支持正向、反向写法：`AI可用: 是`、`[AI可用:: 是]`、`tags: [AI可用]`、正文 `#AI可用`、`no_ai: false` 会被视为可用，`no_ai: true`、`AI不可用: 是`、`tags: [no-ai]`、HTML details 正文里的 `no_ai:: true / AI不可用:: 是 / draft:: true / private:: true / archived:: true` 和 Canvas 文本节点里的 `AI不可用:: 是` 不会进入知识索引或章节上下文；HTML details 正文声明不可用于 AI 或声明过滤状态时只排除该折叠块；Canvas 按节点隔离 AI 不可用内容，同一张画布里的公开节点仍可同步
- [ ] 能确认 Obsidian 状态别名、布尔状态 Properties、标签和目录参与过滤：`status: 正式设定`、`status: official`、`status: final`、`canonical: true`、`published: yes`、正文或 Canvas 文本节点 `canonical:: true`、`tags: [正式, AI可用]`、正文 `#canonical #AI可用`、Canvas 文本节点 `#canonical #AI可用` 和 `正式设定/` 目录笔记能进入知识索引；`status: wip`、`status: archived`、`draft: true`、`private: true`、`archived: true`、正文或 Canvas 文本节点 `draft:: true`、`tags: [草稿, AI可用]`、正文 `#private #AI可用`、Canvas 文本节点 `#draft #AI可用`、`Drafts/` 和 `Private/` 目录笔记不进入知识索引；显式 `status: canonical / final` 优先于布尔状态、标签或目录，显式 `status: wip` 优先于正式布尔属性和正式目录
- [ ] 能确认 `status: canonical # 正式设定`、`chapter_range: 58-60 # 中段` 这类 Obsidian frontmatter 行尾注释不会污染状态过滤、章节范围、图谱关系和写作约束，引号里的 `#` 和双链 heading 会保留
- [ ] 能确认 `tags: "#人物 #第58章 #剧透/57"`、`tags: 人物 主角`、`tags: "人物；第58章"` 和 `tags:: #支线 #第59章` 这类 Obsidian 标签属性会拆成单个标签，并参与章节范围、剧透边界、检索预览和章节上下文过滤
- [ ] 能确认 `tags: #章节/44-45 #剧透/43` 和多行 `tags: - #人物/配角` 这类未加引号井号标签只在标签字段里保留为标签，普通 frontmatter 字段的 `#` 仍按行尾注释处理
- [ ] 能确认正文标签 `#人物／主角` 可参与类型推断，正文 `#适用章节／40～42` 和 `#剧透／39` 可参与章节范围、剧透边界、检索预览和章节上下文过滤
- [ ] 能确认 `chapter_range: 58+`、`chapter_range:: 第59章以后`、`tags: [第58章起]` 和 `tags: [Ch58+]` 会解析成开放式章节范围，早于起始章节不可见，起始章节和后续章节可见；`#第58章` 仍只绑定单章
- [ ] 能确认 `source_notes: "[当前线索](Clues/当前线索.md)"`、`related_characters:: [林追](../Characters/林追.md)`、`[林追](/Characters/林追.md)` 这类关系字段 Markdown 内链会生成关系语义、可解析链接和反向链接
- [ ] 能确认关系字段里的 `#小节` 或 `^block` 会进入“关系小节”上下文和知识检索，内部 links / backlinks 仍按笔记文件记录，指向未来笔记的小节名不会在早期章节预览里显示
- [ ] 能确认 `source_notes: "[线索乙, 潮账](../Clues/线索乙.md)"`、`related_characters: ["[林追, 主角](../Characters/林追.md)"]` 和 `depends_on: "[[线索甲|账册, 初证]]"` 这类关系字段不会因链接标签或双链别名里的逗号被拆成错误目标，仍会生成关系语义、可解析链接和反向链接
- [ ] 能确认 `[source_notes:: [当前线索, 潮标](当前线索.md)]`、`(related_characters:: [林追, 主角](林追.md))` 和 `depends_on:: [[线索甲|账册, 初证]]` 这类 Obsidian / Dataview 正文内联属性不会因嵌套 Markdown 内链或双链别名逗号被截断，仍会生成关系语义、可解析链接和反向链接
- [ ] 能确认 `> - [ ] status:: canonical`、`> - [!] source_notes:: [[当前线索]]`、`> - [?] required_phrases:: 潮声异常` 和 `> ## 必须包含` 下的 `[>] / [/] / [-]` 等常见 Obsidian Tasks 状态任务项会从可见 callout 进入元数据、图谱关系和章节约束；指向未来笔记时早期章节预览会隐藏
- [ ] 能确认 `![[当前线索]]` 和 `![[关系图.canvas]]` 这类笔记嵌入会进入 `embedded_links`，章节上下文显示目标章节可见嵌入笔记的短预览；`![[旧地图.png]]`、`![[访谈.pdf]]`、Markdown 图片和 HTML 媒体标签不会进入图谱或章节上下文，未来笔记嵌入不会在早期章节预览里显示
- [ ] 能确认 Markdown 笔记和 Canvas 里的 HTTP(S) 考据入口会进入 `external_links`，Markdown 链接标题、HTML `<a>` 文本、Canvas link 标签和结构化来源名会进入 `external_references`，同步面板会统计考据链接数量，技能库和架构总览的 Obsidian 笔记卡片、章节上下文优先显示“考据来源”；`source_url / reference_links / research_links / external_links / references / sources / citations / url / 资料链接 / 资料来源 / 参考链接 / 参考资料 / 考据链接 / 考据来源`、正文内联属性、Markdown 外部链接、引用式链接定义、裸 URL、HTML `<a>` 和 Canvas link 节点 URL 均可识别；外部 URL 不进入 Vault 内部 links / backlinks / graph_relations，本地附件链接仍会被过滤
- [ ] 能确认目标章节可见的 Obsidian “考据来源”会出现在任务蒸馏 / Agent 资料分析材料里；修改笔记的 `external_references / external_links` 后，相关蒸馏资料会刷新
- [ ] 能确认 Markdown 笔记、Canvas 文本节点和可见 callout 中的章节计划或章节合同表格会把 `章节目标 / 必须节拍 / 禁写动作 / 验收项 / 证据来源 / 相关人物` 等列转成章节上下文和图谱关系，`章尾留下黑盐仓库选择\|旧账缺页` 这类表格单元格会还原为普通 `|`，表格里的未来笔记在早期章节预览里会隐藏
- [ ] 能确认章节计划或章节合同里的 YAML 对象列表会把 `scenes / required_beats / character_checks / debts_to_advance` 下的 `goal / conflict / payoff / evidence_sources` 等字段转成章节上下文；列表项写成单独 `-` 后换行维护对象字段时也能解析；写成 `required_beats: [{goal: ..., evidence_sources: [{source_note: ...}]}]` 或 `- {goal: ..., payoff: ...}` 这类 YAML flow mapping 时也能解析字段和嵌套证据来源；对象字段值写成 `goal: >`、`reason: |` 这类多行 YAML block scalar 时不会丢失正文；场景项内嵌的 `character_checks / evidence_sources` 对象列表也会递归解析，嵌套列表单独 `-` 后换行写对象字段也能解析；`chapter_contract:` 顶层对象里的 `chapter_range / objective / required_beats / acceptance_checks / evidence_sources` 会进入章节范围、章节上下文和图谱关系；字段里的双链和 Markdown 内链会生成链接、反向链接和图谱关系，指向未来笔记时早期章节预览会隐藏
- [ ] 能确认 `[林追旧档](../Characters/林追(旧).md)` 这类 Markdown 内链括号文件名会解析到完整目标，指向未来笔记时早期章节预览会隐藏链接标签和目标路径
- [ ] 能确认 `[林追旧档](../Characters/林追\(旧\).md "旧档")` 这类 Markdown 内链转义目标会还原文件名，`[终局答案](../Secrets/未来真相 "终局")` 会去掉 title 后解析，指向未来笔记时早期章节预览会隐藏链接标签和目标路径
- [ ] 能确认 `[林追旧档][old]`、`[林追旧档]`、`[old]: ../Characters/林追\(旧\).md "旧档"` 和 `[林追旧档]: ../Characters/林追\(旧\).md "旧档"` 这类 Markdown 引用式链接会生成关系语义、可解析链接和反向链接，指向未来笔记时早期章节预览会隐藏正文引用、定义路径和 title；`[^id]: ...` 脚注定义、缩进续行和正文脚注标记不会生成图谱链接，也不会进入检索预览或章节上下文
- [ ] 能确认 `<a href="../Characters/林追.md">林追</a>` 这类 HTML 链接会生成关系语义、可解析链接和反向链接，指向未来笔记时早期章节预览会隐藏链接标签和路径，指向本地附件时不会进入检索预览或章节上下文
- [ ] 能确认 `obsidian://open?vault=Demo&file=Characters%2F林追.md` 和 `obsidian://advanced-uri?vault=Demo&filepath=Secrets%2F未来真相.md&heading=终局答案` 这类 Obsidian URI 会按 Vault 根路径生成关系语义、可解析链接和反向链接，URI 小节 / 块引用会进入章节安全上下文，指向未来笔记时早期章节预览会隐藏链接标签、路径和关系语义
- [ ] 能确认 Canvas link 节点里的 `obsidian://open` 和 `obsidian://advanced-uri` 不会作为 HTTP(S) 考据来源处理，而是按 Vault 内部链接生成可解析链接、反向链接和图谱关系；无标题内部 URI link 节点作为 Canvas 边端点时图谱关系使用目标笔记名，不显示节点 id；指向未来笔记时早期章节检索和预览不会显示 URI、link 标签、目标路径或关系语义
- [ ] 能确认 `[[../Characters/林追]]`、`[[./当前线索]]`、`[[/Characters/林追]]` 这类 Obsidian 相对路径或根路径双链会按来源笔记路径生成可解析链接、反向链接和关系语义，指向未来笔记时早期章节预览会隐藏标题与别名
- [ ] 能确认 `[[Characters/林%20追]]`、`[[../Secrets/未来%20真相]]` 这类 Obsidian URL 编码双链会先解码再生成可解析链接、反向链接和关系语义，指向未来笔记时早期章节预览会隐藏标题、路径与别名
- [ ] 能确认 `[[Characters/林%23追]]`、`[线索](Secrets/未来%5E真相.md)` 这类 Obsidian URL 编码保留字符不会被误拆成 heading、query 或 block 引用，早期章节预览仍会隐藏后段笔记
- [ ] 能确认根目录笔记里的 `[[../Outside/旧设定]]`、`[[Clues/../../Outside/旧设定]]`、`[[/../Outside/旧设定]]`、`[外部](/../Outside/旧设定.md)` 这类越出 Vault 的相对链接不会生成未解析链接、图谱关系或维护建议，`[[Clues/../Characters/林追]]`、`[[/Characters/林追]]` 或子目录内指回 Vault 的相对路径仍可解析
- [ ] 能确认 Canvas file 节点里的 `../Clues/当前线索.md` 这类相对路径会按 Canvas 所在路径生成可解析链接和反向链接，指向未来笔记时早期章节预览会隐藏节点标题和边标签
- [ ] 能确认 `[[当前线索^block-id]]`、`[[当前线索#小节^block-id]]` 这类 Obsidian 块引用会按对应笔记生成可解析链接和反向链接，指向未来笔记时早期章节预览会隐藏
- [ ] 能确认 `[[当前线索#内部索引]]`、`[[当前线索^scene-a]]`、`[回看](当前线索.md#内部索引)` 这类同笔记内部链接不会生成跨笔记 links、resolved_links、backlinks 或 graph_relations，外部链接仍正常解析
- [ ] 能确认 `summary / description / abstract / keywords / search_terms / 关键词` 等 Obsidian Properties 会进入知识索引和章节上下文选择，摘要和正文里的未来双链、Markdown 内链或已知未来笔记纯文本标题在早期章节预览里会隐藏
- [ ] 能确认 `tags / aliases / keywords / required_phrases / forbidden_phrases` 里的未来双链、Markdown 内链或已知未来笔记纯文本标题 / 路径名 / 文件名，在早期章节上下文、标签预览和写作约束里会显示为“未开放设定”，不会暴露后段笔记标题
- [ ] 能确认 `summary:: / keywords:: / source_notes:: / chapter_range:: / reveal_after_chapter:: / required_phrases:: / forbidden_phrases::` 以及 `[summary:: ...]`、`(chapter_range:: ...)` 等 Obsidian / Dataview 正文内联属性会进入摘要、检索词、图谱关系、章节过滤和写作约束
- [ ] 能确认 `## 来源笔记`、`## 相关人物`、`## 伏笔` 等正文关系小节和 `相关地点：[[旧码头]]` 这类关系行会进入图谱关系、已解析链接和反向链接，并按目标章节隐藏未来关系目标
- [ ] 能确认 Canvas 边指向未来 file 节点时，早期章节上下文不会显示带后段信息的边标签，会改成“未开放关系”；边目标可见但图谱关系标签写到未来笔记名时，也会改写为“未开放设定”
- [ ] 能确认 Obsidian Markdown 和 Canvas 文本里的 `%%...%%` 注释、Markdown HTML 注释 `<!-- ... -->`、fenced code block、inline code、Markdown 删除线、HTML 删除标签和隐藏 HTML details 不会进入图谱关系、检索预览、章节上下文、必写 / 禁写约束和反向链接；`summary / class` 隐藏标记、折叠正文里的 `no_ai:: true / AI不可用:: 是` 和过滤状态都会排除对应 details；普通公开 details 仍会作为正文参与解析
- [ ] 能确认 Obsidian Markdown 和 Canvas 文本里的隐藏 callout 不会进入图谱关系、检索预览、章节上下文、必写 / 禁写约束和反向链接，同一引用块内后续普通 callout 仍隐藏，结束引用块后重新开始的 `note / info` callout 仍会作为正文参与解析；普通 callout 里嵌套的隐藏 callout 只排除嵌套块，外层公开内容仍会作为正文参与解析
- [ ] 能确认 `![[当前线索]]`、`![[关系图.canvas]]` 这类 Obsidian 笔记嵌入会进入图谱关系和反向链接，而 `![[旧地图.png]]`、`![[访谈.pdf]]`、Markdown 图片、普通 Markdown 附件链接、引用式附件链接和 HTML 媒体标签不会进入图谱关系、未解析链接、检索预览或章节上下文
- [ ] 能确认 Obsidian frontmatter 的来源笔记、相关人物和关联笔记字段能形成已解析链接和反向链接
- [ ] 能确认 `depends_on / foreshadows / payoffs / reveals / related_locations / related_props / related_organizations` 等常见 Obsidian Properties 能形成已解析链接、反向链接，并在章节上下文和界面预览里保留关系语义
- [ ] 能确认关系语义在章节上下文里按目标章节过滤，早期章节不会显示指向后段笔记的 `伏笔 -> ...` 等关系标题
- [ ] 能确认当前章节可见笔记正文里的未来 `[[双链]]` 不会进入早期章节上下文、资料分析、任务蒸馏或连续性证据，同时不涉及未来笔记的未解析双链和可见范围内的歧义双链仍会显示为图谱风险
- [ ] 能确认目标章节上下文会丢弃无法对应当前总览可见笔记的 Obsidian 检索命中，旧索引不会把未来设定带入早期章节
- [ ] 能确认模型版架构总览缓存里存在后段 Obsidian 实体时，早期章节生成 / 改稿上下文和项目记忆不会显示该实体
- [ ] 能确认模型版架构总览缓存里存在后段 Obsidian 实体时，续写 / 改稿类项目蒸馏包不会显示该实体；没有目标章节的续写、改稿、仿写和人物任务也不会默认读取后段 Obsidian 笔记
- [ ] 能确认选中早期章节后，在技能库或架构总览执行知识检索时，Obsidian 结果不会显示后段专用笔记或后段正文双链
- [ ] 能确认大量后段 Obsidian 笔记命中同一关键词时，早期章节检索仍能返回当前章节可见笔记
- [ ] 能确认章节生成 / 改稿上下文遇到大量后段 Obsidian 命中时，仍会带入当前章节可见笔记
- [ ] 能确认选中早期章节后执行联网考据时，本地资料命中不会引用后段专用 Obsidian 笔记，整理提示里会标明目标章节
- [ ] 能确认早期章节的连续性证据检索不会返回只因未来双链、Markdown 内链、未来标题或已知未来笔记纯文本标签命中的 Obsidian 候选；安全内容里没有查询词时会丢弃该证据
- [ ] 能打开 Agent 自学习面板并刷新
- [ ] 能在 Agent 自学习面板看到系统学习版文风 / XP 区域
- [ ] 能确认系统学习版文风 / XP 规则变为 `active` 且没有匹配 Vault 规则笔记时，会生成 `Style/` 或 `XP/` Obsidian 待审维护草稿；草稿会写入 `style_rule` 或 `xp_rule` Properties，多来源规则按最晚来源章开放；发布前在目标章节待审草稿提醒和 Agent 规划上下文里显示非正式“文风预览”或“XP预览”，包含规则、适用范围、检查项、证据数和置信度，作者删除 frontmatter 但保留正文规则行时仍能读取；发布后目标章节可见的规则会进入文风 / XP 提示；作者改名、移动或重写规则表达但保留 `source_ids` 时，不会重复生成同一条维护建议
- [ ] 能确认只写 Properties 的 Obsidian 文风 / XP 规则笔记也会进入目标章节提示和 Agent 路由 / 规划上下文：`style_rule / voice_rule / tone_rule / sentence_rhythm / imagery / dialogue_rule / avoid_style / examples / applies_to` 和 `xp_rule / precheck / postcheck / workflow / technique / avoid_xp` 会成为项目级文风 / XP 参考，长属性不会被摘要或短预览截断，后段规则不会出现在早期章节；没有目标章节时只读取无章节边界的全局规则；全局规则数量超过提示容量时，目标章节专属文风和 XP 规则仍会进入提示
- [ ] 能在 Agent 自学习面板看到剧情债务与人物弧线区域
- [ ] 能在 Agent 自学习面板看到章节任务卡里的 Obsidian 任务约束和执行状态
- [ ] 能在 Agent 自学习面板看到章节任务卡里的 Obsidian 考据来源，且 Agent 规划目标章节时能在能力上下文读取同一批来源
- [ ] 能在 Agent 自学习面板看到 Obsidian 维护摘要
- [ ] 能确认 Agent 自学习面板按状态、来源章节或搜索词筛选 Obsidian 维护项时，维护摘要按可见结果重新统计；从 `obsidian_maintenance` 产物卡片打开第 N 章维护队列时，摘要总数与列表显示数一致
- [ ] 能在 Agent 自学习面板看到 Obsidian 维护建议
- [ ] 能看到中高优先级 Obsidian 维护建议自动生成项目内待审草稿
- [ ] 能确认已保存章节没有匹配到 Obsidian 章节档案时，会生成 `ChapterNotes/` 待审草稿，草稿包含 `chapter_index / chapter_title / chapter_summary / handoff_to_next / chapter_excerpt`、Obsidian 必写 / 禁写执行状态、`source_ids`、`source_chapters`、`source_notes` 来源笔记、章节计划、剧情债务、人物弧线和地点 / 道具 / 组织图谱关系，且 `source_notes` 不引用其它章节档案；正文会列出本章 Obsidian 章节计划、剧情债务、人物弧线和下一章交接，发布后后续章节能检索到，早期章节不会提前看到；未发布前，目标章节可见的 `create_chapter_note` 草稿会在章节生成上下文和 Agent 规划上下文显示“草稿预览”，包含章节摘要、下一章交接和未完成 Obsidian 必写项，作者删除 frontmatter 但保留正文小节时仍能显示这些预览，同时标明不能当作 Vault 正式设定引用，人物、剧情债务和图谱等其它草稿不会显示章节档案预览；作者只用 `chapter_summary / chapter_events / state_changes / handoff_to_next / chapter_excerpt` 维护章节档案时，目标章节知识索引、上下文、Agent 能力上下文和叙事状态账本能读取这些 Properties，第 59 章能看到第 58 章档案的交接提醒，第 10 章不能提前看到；作者改名、移动或重写章节档案但保留 `source_ids`、`source_chapters`、`章节来源 / 来源章` 或正文“来源章节 / 来源章”时，不会重复生成同一章档案建议；`source_ids` 写成 `chapter-058`、`chap-058`、`ch058` 或 `Chapter 58` 都能推断来源章节，普通 `archive-ch058` 不会被当作章节；作者删除 `source_ids` 但保留 `source_chapters` 或中文来源章节别名时，移动后的 `author_archive` 也能被维护队列识别为同一章档案；同一档案带多个章节来源 ID 或来源章节时，早于最晚来源章节的章节不能检索到该档案；待审草稿或维护建议只保留 `source_chapters` / 正文“来源章节 / 来源章”时，早于最晚来源章节的目标章节不会看到该维护项；Markdown / Canvas 摘要里能看到解析后的 `source_chapters`，技能库 Obsidian 知识库和架构总览能显示来源章节
- [ ] 能确认章节档案保留 `source_ids` 或 `source_chapters` 时，`第058章-银潮灯回顾.md` 这类文件名不会把档案限制成只在第 58 章可见；后续章节仍能检索到档案
- [ ] 能确认章节核验会读取明确指向当前章的强制类 `handoff_to_next`：第 2 章缺少第 1 章档案交接关键词时形成 Obsidian 必需设定问题并触发自动修订判定，写入关键词后关闭，第 3 章不会重复提示这条第 2 章事项；`下一章关注本章后果` 这类关注提示不会形成 Obsidian 必需设定问题
- [ ] 能确认 Obsidian 待审草稿文件缺失时，章节生成上下文仍按维护项里的 `source_chapters` / 正文“来源章节”过滤；含第 58 章和第 60 章来源的缺失草稿不会出现在第 59 章提示里
- [ ] 能确认作者删除 Obsidian 待审草稿 frontmatter 但保留正文“来源章节”时，章节生成上下文仍按正文来源章节过滤；含第 60 章来源的草稿不会出现在第 59 章提示里
- [ ] 能确认章节档案草稿包含 `source_chapter_hash`，同步后可凭该签名识别对应旧正文的章节档案
- [ ] 能确认多章保存后，早期章节档案仍出现在 Obsidian 长篇维护队列里，不会被前几条建议挤出
- [ ] 能确认未人工改动的自动章节档案草稿会随章节正文刷新；章节标题变化时迁移到新的 `ChapterNotes/` 文件名并移除旧自动草稿，人工改动过的草稿不会被覆盖
- [ ] 能确认自动发布过的章节档案在章节正文或标题变化后显示 Vault 待更新，且章节档案不会把自己列为本章来源笔记
- [ ] 能确认 Vault 待更新项保存新版草稿后会生成 Vault 合并草稿，合并草稿会标出正式 Vault 笔记和系统建议路径，且不会覆盖正式 Vault 笔记
- [ ] 能确认作者在 Obsidian 合并正式笔记后，可在自学习面板单条或批量确认 Vault 已合并，并刷新 Obsidian 摘要和知识库
- [ ] 能看到 Obsidian 待审草稿的自动草稿、人工改动草稿、保留人工草稿、草稿缺失、Vault 笔记缺失、Vault 笔记已移动、Vault 待更新、Vault 合并草稿和已忽略状态
- [ ] 能确认发布过的 Vault 笔记被移动或改名且内容未变时，维护记录会按内容签名改到新的 Vault 路径；新生成的维护笔记带 `gaoxia_maintenance_id`，发布到 Vault 前也会恢复缺失的身份字段，内容被作者改过但身份唯一匹配时也会改到新路径；若对应维护项仍在列表中，会显示移动前后路径；无唯一匹配或文件被删除时仍提示 Vault 笔记缺失并允许重新发布
- [ ] 能在 Agent 自学习面板按状态筛选 Obsidian 维护建议，并按标题、路径、章节或动作搜索
- [ ] 能把当前筛选结果里的 Obsidian 维护建议批量保存为项目内待审草稿，且不会自动发布到 Vault
- [ ] 能把当前筛选结果里已保存的 Obsidian 草稿显式批量发布到 Vault，并确认目标路径不会覆盖已有笔记
- [ ] 能批量忽略当前筛选结果里的 Obsidian 维护建议，并确认这些建议进入已忽略状态
- [ ] 能批量恢复当前筛选结果里的已忽略 Obsidian 维护建议，并确认这些建议重新进入待处理状态
- [ ] 能忽略暂不处理的 Obsidian 维护建议，并确认同路径新建议不会自动生成草稿
- [ ] 能恢复已忽略的 Obsidian 维护建议，并确认同路径新建议可以重新进入待处理和自动草稿流程
- [ ] 能把 Obsidian 维护建议保存成项目内待审 Markdown 草稿
- [ ] 能把 Obsidian 维护建议显式发布到配置的 Vault，并在 Obsidian 同步结果里看到新笔记
- [ ] 能确认未解析双链生成的 Graph 待审草稿会继承来源笔记的章节范围和剧透边界；来源范围不连续或多个开放起点不同时不会合成过宽章节范围，发布到 Vault 后不会进入范围外章节上下文
- [ ] 能确认已解析双链如果来源笔记全书可见或范围更宽、目标笔记在部分来源章节不可见，会生成章节范围不匹配图谱风险，并进入 Obsidian 维护建议和 Agent 规划上下文
- [ ] 能确认两篇以上孤立正式 Obsidian 笔记会生成 `Graph/孤立笔记整理-{来源摘要}.md` 待审索引草稿，发布后原笔记出现 backlinks；旧索引发布后新增孤立笔记仍会生成新的待审草稿
- [ ] 能确认带未解析或歧义双链的笔记不会被计入孤立笔记，也不会额外生成孤立索引维护建议
- [ ] 能确认未人工改动的自动 Graph 草稿会随来源内容和章节边界变化刷新，人工改动过的草稿不会被覆盖
- [ ] 能确认剧情债务和人物状态维护草稿会按来源章节写入剧透边界，多个来源章节时按最晚来源章开放；剧情债务草稿写入 `debt_content / debt_status / risk_level / next_required_action`，人物状态草稿写入 `character / phase / current_state / unresolved_pressure / required_next_check` 并带 `人物状态` 标签；发布到 Vault 后不会进入早期章节上下文，作者改名、移动或改写标题并保留 `source_ids` 后，不会重复显示同一维护建议
- [ ] 能确认早期章节的 Agent 路由 / 规划上下文只显示当前章节可见的 Obsidian 维护摘要和维护建议；第 2 章不会把第 60 章维护项计入待处理、高优先级或自动草稿数量；没有明确目标章节的非架构任务只显示全局维护摘要，不暴露后段建议标题、路径或动作
- [ ] 能确认章节生成上下文和 Agent 路由 / 规划上下文会把来源章节最贴近目标章的 Obsidian 待审草稿排在前面，并显示来源章节
- [ ] 能确认剧情债务草稿的预计处理区间写入 `expected_payoff_range`，不会作为 `chapter_range` 限制中间章节读取
- [ ] 能确认只写 Properties 的 Obsidian 剧情债务和人物弧线笔记也会进入账本：`debt_content / debt_status / risk_level / expected_payoff_range / next_required_action / related_characters` 和 `character / phase / current_state / unresolved_pressure / required_next_check` 会出现在叙事状态、章节任务卡、目标章节提示和 Agent 规划上下文里
- [ ] 能确认模型叙事编辑生成下一章合同时，会读取下一章可见的 Obsidian 剧情债务和人物弧线
- [ ] 能确认模型叙事编辑生成下一章章节合同时，如果没有对应 Vault 章节计划，会生成 `Plans/` 章节合同待审草稿；发布前在目标章节待审草稿提醒和 Agent 规划上下文里显示非正式“合同预览”，包含目标、节拍、禁写动作和验收项，作者删除 frontmatter 但保留合同正文小节时仍能读取；发布后进入目标章节的 `Obsidian 章节计划`，保护债务、禁止动作和验收项不会被截断；作者只用 Properties 维护 `objective / required_beats / forbidden_moves / acceptance_checks / evidence_sources` 时，目标章节提示仍能读取目标、节拍、禁写动作和验收项，证据来源能形成图谱关系；作者改写已发布合同正文并保留 `gaoxia_maintenance_id` 或 `source_ids` 后，不再重复显示同一维护建议
- [ ] 能确认 Agent 路由 / 规划上下文会读取目标章节可见的 Obsidian 来源、章节计划、必写项、禁写项、剧情债务、人物弧线和图谱风险
- [ ] 能确认 Agent 路由 / 规划上下文会识别“第 58 到 60 章”这类范围目标，并同时展示多个目标章节的 Obsidian 任务摘要；“先检查第一章，再生成第二章”会优先使用生成目标；包含“蓝图 / 架构”等词但明确写了章节范围时，也会优先读取目标章节 Obsidian 任务；明确要求生成正文的 2 到 3 章范围会生成逐章计划动作，超过 3 章会提示分批或先整理章节蓝图；确认执行后跨到下一章时会按新目标章刷新资料库和 Obsidian 分析摘要
- [ ] 能确认 Agent 执行中点击停止会写入 workflow 中断请求，后端在动作边界把后续动作标记为 `CANCELLED`；连接异常后可用 `task_id` 读取 workflow 摘要并恢复 action / subtask 时间线
- [ ] 能确认 Agent 章节生成、draft 写回或改稿写回后，如果生成或更新了 Obsidian 维护草稿，结果区会显示 `obsidian_maintenance` 产物，并列出相关待审草稿、状态和路径
- [ ] 能确认 `obsidian_maintenance` 产物卡片可打开 Agent 自学习面板，并按来源章节和产物 `metadata.suggestion_ids` 筛选 Obsidian 维护项；界面显示“清除产物筛选”入口后，清除筛选能回到该章节维护队列
- [ ] 能确认 Agent 自学习面板最新章节任务卡会展示目标章节可见的 Obsidian 剧情债务和人物弧线
- [ ] 能确认带章节范围或剧透边界的 Obsidian 待审草稿不会提前进入范围外章节生成上下文；作者手工改动草稿文件 frontmatter 后，章节提示按真实草稿文件过滤，字段名使用常见属性写法时也能生效；`tags: [第58章起, 剧透/57]`、`tags: [Ch58+]` 和多行 `tags:` 列表会按正式笔记相同的标签章节范围和剧透边界生效；待审草稿提示会逐条读取对应草稿并校验维护 ID / 类型，不会把其它草稿正文拼到当前维护项
- [ ] 能确认只有 `reveal_after_chapter` 或 `#第57章后可用` 的 Obsidian 章节档案，在第 58 章及后续章节选择资料时优先于普通全局资料
- [ ] 能在 Agent 自学习面板看到模型叙事审查和章节合同信息
- [ ] 能在 Agent 自学习面板运行写作回归和模型审查
- [ ] 能在 Agent 自学习面板查看趋势、失败案例和技能版本区域
- [ ] 能保存自学习排程设置，并手动执行一次排程任务
- [ ] 能在设置页高级区看到第二审查模型配置
- [ ] 能在设置页高级区看到运行调度配置
- [ ] 能保存人物卡并再次载入
- [ ] 能导出整本

## 模型相关

- [ ] 能保存简化后的模型配置
- [ ] 在 AI 写作设置页复制、选择或编辑服务商、模型名、接口地址、API Key 等输入内容时，设置页不会自行关闭；点击“关闭”按钮后能正常关闭
- [ ] 设置页不再显示 Embedding 配置入口，“测试当前配置”里的知识检索模型使用内置本地模型并能通过
- [ ] 能看到模型任务状态或队列提示
- [ ] 能正常发起生成
- [ ] 项目记忆里写入“硬规则 / 警告”禁写表达后，章节正文命中时核验报告能显示 `项目记忆规则` 风险，并触发自动修订判断；重点检查“不要提前揭示某人是主谋”“某人不能被提前揭示为主谋 / 真凶 / 卧底 / 潮师”、“铜钥匙不能被交给白石商会”、人物改名、关键人物死亡 / 叛变和主动暴露身份；人物改名要同时检查“A 被改成 / 写成 / 叫成 B”会报风险，B 作为正常角色名出现不会误报；也要检查“没有暴露身份”“并不是主谋”“并不是卧底”“没有把铜钥匙交给白石商会”这类否定状态表述不会被误报；修改项目记忆后，旧章节核验报告应标记为过期，刷新后按新规则重查
- [ ] 架构总览的“章节核验”页签能显示每章核验分数、状态、维度、问题、建议和过期标记，`项目记忆规则` 触发项能在对应维度里看到

## 问题记录

### 1. 问题现象

-

### 2. 复现步骤

1.
2.
3.

### 3. 报错原文或截图说明

-

### 4. 是否稳定复现

- [ ] 每次都出现
- [ ] 偶尔出现
- [ ] 只出现过一次
